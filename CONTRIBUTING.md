# 投稿指南

这份报纸的内容就是一条条案例。**贡献这个项目，主要就是贡献案例。**

一条案例 = `content/stories/` 里的一个 JSON 文件（＋一张可选的配图）。没有数据库，也没有 CMS，不用在别处登记：文件放进去，页面就有了。

---

## 两种投稿方式

### 1. 只提线索：开一个 Issue（最省事）

看到一篇值得收录的报道，但不想动代码：

> [提名一条案例 →](../../issues/new?template=new-story.yml)

把原文链接和几句话填进表单就行，剩下的由维护者整理。

### 2. 直接提 PR：新增一个案例文件

会 Git 的话，这条路最快被合并。三步：

```bash
pnpm install
# 1. 新建 content/stories/<编号>.json（见下面的字段说明）
# 2. 可选：把配图放进 public/stories/<编号>.jpg
pnpm check      # 自检：字段、链接、配图、编号
pnpm dev        # 打开 http://localhost:3000/story/<编号> 看一眼
```

自检通过、页面正常，就可以提 PR 了。

> `pnpm dev` 和 `pnpm build` 启动前会先按全部案例的文字重新裁一遍字体，生成的文件（`app/fonts/generated/` 和 `app/fonts/huiwen-supplement.css`）已被 Git 忽略，不会出现在你的改动里，也不需要提交。

---

## 怎么写一个案例文件

### 编号

`content/stories/` 里最大的编号 +1，文件名是**三位数字 + `.json`**，例如 `216.json`。这个编号就是它的网址：`/story/216`。

如果提 PR 时发现编号被别人占了，往后顺延一个即可，不必重做。

### 一个完整的例子

`content/stories/216.json`：

```json
{
  "title": "16岁高中生用Cursor写了个情绪记录App，8个月月入过万",
  "section": "prodigies",
  "outlet": "新浪科技",
  "published": "2026-03-18",
  "url": "https://example.com/the-original-report",
  "image": "216.jpg",
  "summary": "他没有编程基础，用 Cursor 边问边写，8 个月做出一个情绪记录 App。报道里能确认的只有下载量和订阅价格，月入过万是他本人的说法。",
  "quote": "当手中工具足够锋利时，莽撞的开始比完美的计划更有力量。",
  "coverage": [
    {
      "title": "16岁高中生自学编程月入过万",
      "outlet": "网易",
      "published": "2026-03-20",
      "url": "https://example.com/a-reprint"
    }
  ]
}
```

### 字段说明

页面上出现的每一样东西，都在这张表里；表里没有的字段会被自检拦下来。

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `title` | ✅ | 本报给这条案例起的标题。可以重写原标题，但**不要夸大原报道没有的数字**。标题里的数字和英文会自动上色。 |
| `section` | ✅ | 版块，见下面[版块一览](#版块一览)。 |
| `outlet` | ✅ | 首发或转载的媒体名。带转载说明也行（`"每日经济新闻，腾讯新闻转载"`），报头只取第一段。 |
| `published` | ✅ | 发布时间。`2026-03-18` / `2026-03` / `2026 年` 都可以；实在查不到，写清楚情况（会显示成「日期不详」）。 |
| `url` | ✅ | 原文链接。确实找不到原文时写 `null`，不要留空字符串。 |
| `summary` | ✅ | 两三句话：报道说了什么、哪些数字是可查的、哪些只是当事人自述。这段是搜索结果和分享卡片里的描述。 |
| `originalTitle` |  | 原报道自己的标题。当 `title` 是译写或改写时填，故事页会一并列出。 |
| `image` |  | `public/stories/` 里的文件名，例如 `"216.jpg"`。见[配图](#配图)。 |
| `quote` |  | 一句**从原文里一字不改抄下来**的话。没有真正有力的句子就不要填，宁缺毋滥；不要自己编，也不要改写。 |
| `quoteOriginal` |  | 当 `quote` 是从外语翻译过来时，把原文原话放这里，页面会两句都印。 |
| `coverage` |  | 同一件事的其他报道，见[一件事，一条案例](#一件事一条案例)。 |

### 配图

* 放在 `public/stories/`，文件名跟编号一致：`public/stories/216.jpg`。
* 支持 `.jpg` / `.jpeg` / `.png` / `.webp`。宽度 1200px 以内、单张 300KB 以内就够用，页面会自动裁成 3:2。
* 用原报道自己的配图，不要用与报道无关的网图，也不要用 AI 生成的假照片。
* 没有合适的图可以不配，`image` 字段留空即可 —— 版面对无图的案例有单独的排法。

### 版块一览

| slug | 版块 | 收录什么 |
| --- | --- | --- |
| `prodigies` | 新手村 | 未成年人、学生、刚入行的人 |
| `solo` | 单人速通 | 一个人做完一整个产品/公司 |
| `side-hustle` | 支线任务 | 有本职工作，用 AI 做副业 |
| `virtual` | 皮套人 | 数字人、AI 模特、虚拟主播 |
| `drama` | 开新番 | AI 短剧、网文、音乐等内容工业 |

拿不准就挑最接近的一个，维护者会在合并时调整。想新增版块请先开 Issue 讨论 —— 加一个版块会改动整张报纸的导航。

### 一件事，一条案例

同一件事常常被好几家媒体写过。**不要为每篇报道各建一个文件**，选一篇最完整的做主条，其余的放进 `coverage`：

```json
"coverage": [
  { "title": "转载方的标题", "outlet": "网易", "published": "2026-03-20", "url": "https://..." },
  { "title": "只有一个标题和链接的相关报道", "url": "https://..." }
]
```

`title` 必填，`outlet`、`published`、`url` 都可以省略。填了媒体或日期，页面会在标题下面多印一行报头。

如果你发现库里已经有同一件事的案例，直接给那个文件补 `coverage` 就是很好的 PR。

---

## 收录标准

这份报纸记的是**AI 时代的造富传说**，也记这些传说是怎么被讲出来的。所以：

* **必须有可追溯的原始报道。** 只有朋友圈截图、只有短视频口述、只有卖课广告的，不收。
* **数字要标明来源。** 「月入 5000 万」是媒体核实的、是当事人自述的、还是软文里写的，请在 `summary` 里说清楚。这比数字本身重要。
* **不做二手加工。** `quote` 必须一字不差来自原文。宁可不填，也不要润色。
* **不收纯广告。** 品牌通稿、课程推广、厂商 case study，除非报道本身有独立观察，否则不收。
* **不收纯融资新闻。** 融资额不是收入。
* **不收以比赛获奖为主要看点的报道。** 正常参赛、获奖和领取奖金是值得鼓励的成就，并不是这份报纸要讽刺的“AI 造富神话”。比赛或黑客松可以是故事背景，但案例必须另有明确的造富噱头、夸张数字或值得审视的叙事。

被质疑的、明显夸大的案例并不会因此被排除 —— 这份报纸本来就是在记录这种叙事。

---

## 改一条已有的案例

* **改字、补链接、补配图**：直接改 `content/stories/<编号>.json`，提 PR。
* **发现是重复的**：把它的内容并进主条的 `coverage`，再删掉重复的文件。
* **要下架一条**：删掉对应文件，并在 PR 里说明理由。

---

## PR 检查清单

提交前过一遍：

- [ ] `pnpm check` 通过
- [ ] `pnpm build` 通过
- [ ] `quote` 是从原文一字不改抄来的（或干脆没填）
- [ ] `summary` 说清了数字的来源
- [ ] 确认库里没有同一件事的案例（可以在仓库里搜一下人名或链接）
- [ ] 配图来自原报道，且已放进 `public/stories/`

一个 PR 一条案例最容易合并。批量投稿也可以，只是审得慢一些。

---

## 代码贡献

版式、组件、构建相关的改动同样欢迎，提 PR 前请先开 Issue 说一下想法，避免和正在改的版面撞车。

几个可能需要知道的地方：

| 位置 | 作用 |
| --- | --- |
| `content/stories/` | 所有案例 |
| `app/_data/stories.ts` | 读取 `content/`，把案例交给页面 |
| `app/_data/sections.ts` | 版块清单 |
| `app/_data/front-page.ts` | 头版排版：哪几条上头条、上中栏、上右栏 |
| `app/_components/` | 报头、导航、导语块、各种稿件卡片 |
| `scripts/check-content.mjs` | `pnpm check` 的全部逻辑 |
| `scripts/build-font-subsets.mjs` | `pnpm fonts`：按站内用到的字裁出汇文明朝体子集，`dev` / `build` 前自动运行 |

**头版由维护者排。** 新案例会自动出现在对应版块和版块横栏里，`front-page.ts` 里那几个手挑的位置不用改 —— 想推荐某条上头版，在 PR 里说一声就好。

## 行为准则

就事论事，对稿不对人。报道可以被质疑，人不该被围攻；案例里的当事人多数是普通人，请不要在 Issue 或 PR 里贴他们的私人信息。
